# سجل التعديلات — إعادة البناء النظيفة

هذا الملف بيوثّق بالتفصيل كل فرق بين المشروع الجديد وملفات
`yousef-salon-app-main (2).zip` / `Yousef-salon-booking-main (2).zip` اللي
اعتُبرا المصدر الأساسي. أي حساب Claude تاني (أو أنت شخصيًا) يقدر يرجع لهذا
الملف يفهم منه كل قرار اتخذ وليه.

**المصدر:** نسخة `main` فقط. نسخة `updated` (الأربعة zips التانية) اتفُحصت
ومقارنتها موثقة تحت — بعض إصلاحاتها الحقيقية اتنقلت (موضح ليه كل واحدة)،
وميزة واحدة جديدة (جرس إشعارات الإدارة + صوت) اتأجلت عمدًا (موضح تحت).

---

## 1. فصل كامل عن Firebase القديم

- **`admin-app/src/firebase.js`** و **`customer-booking-app/src/firebase.js`**:
  اتشالت كل بيانات مشروع `abo-yousef-barbershop-63739` (apiKey, projectId,
  إلخ) — كانت موجودة **حرفيًا نفسها في الأربع ملفات الأربعة** (main و updated
  في المشروعين). استُبدلت بقيم placeholder (`REPLACE_WITH_YOUR_...`) مع
  تعليق واضح `NEW FIREBASE PROJECT CONFIGURATION` يشرح منين تجيب القيم
  الجديدة ولازم الاتنين (admin-app و customer-booking-app) ياخدوا نفس القيم
  بالظبط (مشروع Firebase واحد مشترك).
- لا يوجد أي project ID أو API key أو بيانات حقيقية من المحل القديم في أي
  ملف في هذا المشروع.

## 2. إصلاح باج حرج — على الأرجح هو سبب فقدان قاعدة البيانات القديمة

**الملف:** `admin-app/src/App.jsx`, دالة `usePersistentState`.

**قبل:** لو فشل الاتصال بـFirestore (`onSnapshot` error callback) — سواء
بسبب فصل نت مؤقت أو أي مشكلة اتصال لحظية — كان الكود بينفّذ:
```js
setData(seedFactory()); // بيانات فاضية/تجريبية
```
وبما إن أي تغيير في `data` (حتى الناتج عن هذا الفرع نفسه) بيتكتب تلقائيًا
فوق Firestore في `useEffect` تاني (`setDoc(ref, { value: data })`)، فكانت
النتيجة إن **فشل اتصال لحظي واحد كافي إنه يمسح البيانات الحقيقية في
Firestore ويستبدلها ببيانات فاضية أو تجريبية**، بدون أي تحذير للمستخدم.

**بعد:** فرع الخطأ دلوقتي **لا يعمل أي شيء** — لا `setData`، لا `setDoc`.
آخر بيانات محمّلة بنجاح تفضل زي ما هي في الشاشة. لو حصل الخطأ قبل أي تحميل
ناجح، الشاشة تفضل في وضع "جاري التحميل" (الموجود بالفعل في الكود) لحد ما
الاتصال يرجع، بدل ما تفتح ببيانات فاضية وهمية.

**الأثر:** فشل الاتصال بقى لا يساوي أبدًا "امسح البيانات" — بالظبط زي ما طلبت
في نقطة 14 من الطلب الأصلي.

## 3. حذف كود ميت غير مستخدم (وليس ميزة)

**الملف:** `admin-app/src/App.jsx`.

اتشال بالكامل: دالة `buildSeed()` + الثوابت `SEED_NAMES` (30 اسم وهمي)،
`SERVICE_SEED` (20 خدمة وهمية)، `PRODUCT_SEED` (منتجات وهمية) — حوالي 104
سطر. تم التأكد بـ`grep` على المشروع كله إن **مفيش أي مكان بيستدعي
`buildSeed()`** — كانت كود ميت 100%، مش feature شغالة. البيانات الافتراضية
الفعلية للموظفين/العملاء/المبيعات/المنتجات كانت بالفعل `[]` (مصفوفة فاضية)
في `usePersistentState("bp_staff", () => [])` وأخواتها — دي فضلت زي ما هي،
مفيش تغيير عليها.

بالإضافة لذلك، اتشالت دالة `resetAllData()` (غير مستخدمة أبدًا في أي زر أو
حدث — الوظيفة الفعلية لمسح البيانات هي `doReset()` داخل `SettingsTab`) وكذلك
`useEffect` الخاص بمتغير `seeded` (كان مرتبطًا أصلاً بمنطق `buildSeed()`
القديم؛ بعد حذفه أصبح هذا الـeffect بدون أي أثر فعلي — بيحسب متغير محلي
`alreadyLinked` ولا يستخدمه في أي مكان).

## 4. تنظيف `bp_settings` الافتراضية

**قبل:** `salonName: "بارستوديو الملوك"، ownerName: "المالك"` — اسم وهمي
تجريبي كان بيظهر فعليًا كواجهة افتراضية.

**بعد:** `salonName: "", ownerName: ""` — فاضي تمامًا، المالك يدخله بنفسه من
شاشة الإعدادات أول مرة يفتح فيها التطبيق.

**كذلك:** اتشالت 3 حقول ميتة تمامًا من الكائن الافتراضي ومن شاشة الإعدادات:
`openHour`, `closeHour`, `slotMinutes`. تم التأكد بـ`grep` على المشروع كله
إنها **مش مقروءة في أي مكان غير شاشة الإعدادات نفسها** — بقايا من نظام حجز
بمواعيد ثابتة قبل ما يتحول المشروع لنظام "طابور ذكي بدون مواعيد ثابتة" (كما
هو موضح في تعليق الكود الأصلي نفسه). ساعات العمل الفعلية تتحدد من "الفترات"
(periods) في إعدادات الطابور، وهي المستخدمة فعليًا في كل مكان.

## 5. إصلاح باج حقيقي — نقاط الولاء كانت شكلية

**الملف:** `admin-app/src/App.jsx`, دالة `POS.checkout()`.

**قبل:** `pointRate` كان معروض في شاشة الإعدادات وبيتحفظ في Firestore، لكن
حساب النقاط الفعلي كان دايمًا `Math.round(finalTotal)` — نقطة واحدة لكل جنيه
بغض النظر عن قيمة `pointRate` المحفوظة. الإعداد كان شكليًا 100%.

**بعد:** `Math.round(finalTotal / pointRate)` — الإعداد بقى بيُطبَّق فعليًا.

## 6. تفعيل `noShowGraceMinutes` فعليًا

**الملف:** `admin-app/src/App.jsx`, `QueueTab`.

كان الحقل موجود في الإعدادات ومعروض، لكن مفيش أي كود بيقرأه. أُضيف `useEffect`
بيفحص كل 20 ثانية (طول ما تبويب الطابور مفتوح) أي حجز في حالة "تم استدعاؤه"
(`CALLED`) لمدة أطول من المهلة المحددة، ويحوّله تلقائيًا لـ"لم يحضر"
(`NO_SHOW`).

**حد معروف:** هذا client-side فقط — لا توجد Cloud Function مجدولة في
المشروع تُشغّله في الخلفية. لو كل أجهزة الإدارة مقفولة، التحويل هيتلحق فورًا
أول ما حد يفتح تبويب الطابور تاني، مش في نفس لحظة انتهاء المهلة بالظبط.

## 7. تفعيل قسم "التحكم في تطبيق حجز العملاء" في شاشة الإعدادات

**الملف:** `admin-app/src/App.jsx`, `SettingsTab`.

قسم جديد كامل بيحفظ في نفس حقول `bp_settings`: فتح/قفل الحجز، أقصى عدد
حجوزات يوميًا، السماح باختيار الصنايعي، عرض رقم الدور/عدد المنتظرين/الوقت
التقريبي، السماح بالإلغاء الذاتي، وعتبة تنبيه "اقترب دورك". كل حقل هنا
**مطبَّق فعليًا** في تطبيق حجز العملاء (انظر البند التالي) — مش عرض شكلي.

## 8. تفعيل إعدادات الحجز فعليًا في `customer-booking-app`

**الملفات:** `customer-booking-app/src/firebase.js`, `src/lib/queue.js`,
`src/App.jsx`.

### 8.أ إصلاح باج حرج: التطبيق ما كانش بيسجل دخول خالص

`firebase.js` كان بيصدّر `auth` بدون أي استدعاء تسجيل دخول. مع
`firestore.rules` الجديدة (والقديمة أيضًا فعليًا، لأنها كانت تشترط
`request.auth != null`)، هذا يعني إن تطبيق حجز العملاء **كان سيفشل تمامًا**
في قراءة أو كتابة أي شيء في Firestore بمجرد ما حد ينشر rules تشترط تسجيل
دخول. أُضيفت `ensureAnonymousAuth()` باستخدام `signInAnonymously`، والتطبيق
كله بقى معلّق على نجاحها قبل أي render حقيقي (شاشة تحميل + شاشة خطأ واضحة
لو فشل).

**⚠️ يتطلب منك:** تفعيل **Anonymous Authentication** من Firebase Console
→ Authentication → Sign-in method، وإلا تطبيق حجز العملاء لن يعمل إطلاقًا.

**ملاحظة أمان مهمة (موثقة كتعليق في الكود):** Anonymous Auth بتثبت وجود
جلسة متصفح فقط، **وليس** أن العميل فعلاً صاحب رقم الهاتف الذي كتبه. عزل
حقيقي لبيانات كل عميل يتطلب Phone Number (OTP) Authentication بدلًا من ذلك
— تغيير أكبر غير مطبق في هذه النسخة.

### 8.ب تفعيل الإعدادات المُعلنة فعليًا

- `bookingOpen === false` يقفل شاشة الحجز فعليًا (رسالة واضحة للعميل).
- `allowEmployeeSelection === false` يخفي اختيار الصنايعي ويستخدم
  `pickAutoStaff()` (دالة جديدة في `queue.js` تختار الصنايعي الأقل ازدحامًا
  فعليًا من الطابور الحقيقي — وليس عشوائيًا).
- `maxBookingsPerDay` يُفحص داخل نفس Firestore Transaction الخاصة بالحجز
  (يمنع تجاوز الحد حتى مع حجزين في نفس اللحظة).
- `showQueueNumber` / `showPeopleAhead` / `showEstimatedTime` /
  `allowCustomerCancel` تتحكم فعليًا في شاشة تتبع الدور.
- إشعار "اقترب دورك" مبني على `aheadCount` الحقيقي القادم من Firestore
  اللحظي (وليس `setTimeout`)، ويتفعل عند عبور `approachingTurnThreshold`
  القابل للتعديل من الإعدادات.

## 9. `firestore.rules` جديدة وآمنة (المشروعين)

**قبل (main):**
```
allow read, write: if request.auth != null;
```
على **كل مستند في المشروع بلا استثناء**. بمجرد ما عميل (Anonymous Auth) يبقى
"مسجل دخول"، كان عنده صلاحية كاملة قراءة وكتابة على رواتب الموظفين والفواتير
وكل بيانات الإدارة الحساسة.

**بعد:** قواعد مفصّلة بتفرّق بين:
- `isStaff()`: تسجيل دخول بريد/كلمة مرور (owner/manager/cashier/barber).
- `isCustomer()`: تسجيل دخول Anonymous (تطبيق العملاء).

العميل يقدر يقرأ بس المستندات العامة (`bp_services, bp_staff, bp_settings,
bp_queue, bp_duration_stats`)، ويكتب بس في `bp_queue` بإضافة عنصر واحد أو
تعديل بنفس حجم القائمة تقريبًا (فحص شكل التغيير، موضح بالتفصيل كتعليقات في
الملف نفسه). كل حاجة تانية (رواتب، فواتير، مصروفات...) **ممنوعة تمامًا** على
أي عميل.

**قيود معروفة وموثقة صراحة داخل `firestore.rules` نفسها (بدل التظاهر بأمان
كامل غير موجود):**
- فصل owner/manager/cashier على مستوى الـrules نفسه غير مطبق (الأدوار
  الثلاثة عندها نفس صلاحية الكتابة كموظف) — يحتاج Firebase Custom Claims أو
  إعادة هيكلة `bp_team` لمستند لكل موظف.
- عزل بيانات `customerPoints`/`customerBookings` بالكامل بين العملاء يحتاج
  Phone OTP بدل Anonymous Auth (نفس القيد المذكور في البند 8.أ).
- الحماية على `bp_queue` تفحص شكل التغيير (حجم القائمة) فقط، مش هوية العنصر
  المتغير بالضبط — إغلاق هذه الثغرة بالكامل يحتاج Cloud Function كوسيط
  كتابة وحيد، أو نقل كل حجز لمستند مستقل.

## 10. `FIRESTORE_SCHEMA.md`

ملف جديد كامل يوثق كل collection/document المستخدمة في المشروعين، شكل
البيانات، ومن يقدر يقرأ/يكتب. انظر `/FIRESTORE_SCHEMA.md`.

## 11. ما تم فحصه من نسخة `updated` ولم يُنقل (تأجيل، وليس رفض)

نسخة updated أضافت **جرس إشعارات إدارة حقيقي + صوت تنبيه** (مستند جديد
`bp_admin_notifications`، مكوّن `TopBar` جديد، صوت Web Audio API، إلخ) في كل
من app1 و app2. هذه إضافة **ميزة كاملة جديدة**، وليست إصلاح باج أو تفعيل
إعداد معلن مسبقًا — فتم استبعادها من هذا البناء الأساسي عمدًا للحفاظ على
نطاق التغيير محصورًا في: (أ) الفصل عن Firebase القديم، (ب) إصلاح الأعطال
الحقيقية، (ج) تفعيل الإعدادات المعلنة أصلاً في الواجهة. هذه الميزة متاحة
لإضافتها لاحقًا كطلب منفصل إذا رغبت.

## 12. لم يتم اختباره فعليًا (قيد بيئة العمل، وليس Bug)

بيئة إعادة البناء هذه **بدون اتصال إنترنت** (network مقفول بالكامل)، لذلك:

- **لم يُنفَّذ** `npm install` أو `npm run build` فعليًا على أي من المشروعين
  — لا يوجد وصول لـ npm registry من هذه البيئة.
- **بديل ما تم تنفيذه:** فحص syntax/JSX كامل باستخدام
  `tsc --noEmit --jsx react` على كل ملفات `.jsx`/`.js` المعدَّلة في
  المشروعين، ونتيجته **صفر أخطاء syntax** (بخلاف رسائل "cannot find module"
  المتوقعة لغياب `node_modules`، وهي متجاهلة لأنها غير متعلقة بصحة الكود).
  هذا فحص حقيقي ومفيد (يكشف أقواس ناقصة، JSX غير متوازن، أخطاء import) لكنه
  **ليس بديلاً كاملاً** عن `vite build` فعلي.
- **لم يُختبر فعليًا** الاتصال بمشروع Firebase حقيقي، لأنه غير موجود بعد —
  ده يحتاج منك تعمل الخطوات في `README.md` أولاً (إنشاء مشروع Firebase
  جديد، وضع القيم في `firebase.js`، نشر `firestore.rules`، تفعيل
  Authentication) ثم تجربة فعلية من جهازك.

**المطلوب منك بالتحديد قبل ما تعتبر المشروع جاهز:**
1. اعمل مشروع Firebase جديد واحصل على القيم ثم حطها في كلا ملفي
   `firebase.js` (نفس القيم بالظبط في المشروعين).
2. فعّل Email/Password Authentication (للموظفين) و Anonymous Authentication
   (لتطبيق العملاء) من Firebase Console.
3. انشر `firestore.rules` من Firebase Console → Firestore Database → Rules.
4. من كل مشروع: `npm install && npm run build` — تأكد إنه بينتهي بدون أخطاء.
5. `npm run dev` على كلا المشروعين، وجرب سيناريو كامل: دخول موظف → أول
   تسجيل دخول (يصبح owner تلقائيًا) → إضافة خدمة وموظف من الإعدادات → افتح
   تطبيق العملاء واعمل حجز → تأكد إنه يظهر في تبويب "الطابور".
